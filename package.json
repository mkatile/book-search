{
  "dependencies": {
    "@apollo/client": "^3.11.4",
    "apollo-server-express": "^3.13.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "graphql": "^16.9.0"
  },
  "scripts": {
    "start-server": "cd Develop/server && npm start",
    "start-client": "cd Develop/client && npm start",
    "start": "npx concurrently \"npm run start-server\" \"npm run start-client\"",
    "test": "echo \"Write tests!\" && exit 0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
